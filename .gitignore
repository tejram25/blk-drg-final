# Node / Angular
node_modules/
dist/
coverage/
**/.angular/cache/

# Java / Maven
target/

# H2 database files (generated at runtime; never commit DB state or its lock file)
data/
**/data/*.db
# ...but the Flutter app's clean-architecture 'data/' layers are source code.
!diagram-poc/mobile/lib/**/data/
!diagram-poc/mobile/lib/**/data/**

# Logs & OS cruft
*.log
.DS_Store
Thumbs.db

# Local secret overrides (never commit real credentials)
**/application-local.properties
